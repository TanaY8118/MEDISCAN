MEDISCAN BACKEND AUDIT LOG - 2026-01-24

1. DTO CONSOLIDATION (v1)
- CRITICAL: Established 'com.mediscan.dto.v1' as the single public API namespace.
- REFACTOR: Moved 15+ DTOs across 8 modules (Medicine, Inventory, Reminders, etc.) into the versioned namespace.
- ENFORCEMENT: Applied @Value (immutability) and @JsonPropertyOrder to all DTOs for mobile stability.
- IMPACT: Mobile clients now have a frozen contract; backend internal changes no longer break the frontend.

2. BOUNDARY ENFORCEMENT & ENTITY ISOLATION
- REFACTOR: services now handle mapping (Entity -> DTO).
- AUDIT: verified zero 'com.mediscan.*.document' or '.entity' types are exposed in REST Controller method signatures.
- CLEANUP: Purged all legacy local 'dto' and 'response' packages to eliminate technical debt.

3. SECURITY HARDENING (Option B)
- CHANGE: Modified 'SecurityConfig.java' to protect '/api/v1/scan/**' routes.
- RATIONALE: Satisfies Data Ownership Invariant; prevents unauthenticated access to medicine lookup/OCR services.
- VERIFICATION: Confirmed 401 Unauthorized behavior for unauthenticated requests.

4. INITIALIZATION & COLD-BOOT FIXES
- FIX: Resolved 'ConflictingBeanDefinitionException' caused by duplicate 'GlobalExceptionHandler' beans in 'common.advice' and 'common.exception'.
- CLEANUP: Deleted redundant 'com.mediscan.common.advice' package.
- FIX: Applied safe local defaults to 'application.yml' for MYSQL_USER, MYSQL_PASSWORD, and MONGO_URI to ensure context initialization works without external env vars.

5. OBSERVABILITY
- STATUS: 'spring-boot-starter-actuator' is verified.
- HEALTH: validated that healthcare endpoints cover both MySQL (JPA) and MongoDB persistence layers.

REMAINING ACTIONS:
- BLOCKED: Mobile app requires 401 interceptor in 'client.ts' to handle session expiry gracefully (identified during PE audit).

LOG END.
